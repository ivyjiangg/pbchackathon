# Proof Artifacts

Store strict devnet payment evidence files here.

- `devnet-proof.json` is produced by `npm run proof:devnet`.
- Commit at least one successful artifact (`status: 200`) for submission readiness.
