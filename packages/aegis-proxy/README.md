# aegis-proxy

Express-based HTTP proxy for **x402** (HTTP 402) flows on **Solana**. For how to run it from the monorepo root, see the [root README](../../README.md). **Devnet / RPC env vars** are documented there under *Devnet (local proxy)*.
